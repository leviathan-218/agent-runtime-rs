# agent-runtime-rs

Rust 学习项目。

对照 [AgentScope Java](https://github.com/agentscope-ai/agentscope-java) 的架构，用 Rust 从零逐步实现一套 Agent Runtime。不是把 Java 代码翻译过来，而是借它的分层（Message → Model → Tool → Agent → …）来学 Rust。

目前做到 Message 和 Model。
